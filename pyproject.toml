[project]
name = "aerys-platform"
version = "0.1.0"
description = "Aerys platform core modules for AgentLab eval sandbox"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
