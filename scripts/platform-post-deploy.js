#!/usr/bin/env node
'use strict';

/**
 * Post-deployment hooks executed when platform.status becomes ready.
 */

console.log('POST_DEPLOY: platform ready — no external integrations configured for sandbox.');
